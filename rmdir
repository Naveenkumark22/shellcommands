rmdir name	            Remove the directory named name (only if it's empty).

rm filename	            Remove the specified file.
rm -i filename	        Prompt before removing each file (interactive mode).
rm -f filename	        Force removal without prompting (use with caution).
rm -r directory	        Recursively remove a directory and its contents.
rm -r -i directory	    Prompt before removing each directory and its contents.

eg:
single file removal
rm file.txt

multiple file removal
rm file1.txt file2.txt
